# 导出时使用与训练一致的基座模型。
model: Qwen/Qwen3.5-4B

# 训练完成后，把这里改成实际存在的 LoRA checkpoint 路径。
adapters:
  - ./output/qwen35_lora/checkpoint-best

# 合并 LoRA 权重并保存完整模型。
merge_lora: true
output_dir: ./output/qwen35_merged
